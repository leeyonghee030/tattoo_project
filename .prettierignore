# 빌드 산출물
.next/
build/
out/
node_modules/
pnpm-lock.yaml

# 도구 운영 상태 (사람이 편집하지 않는 파일)
.omc/
.gradle/

# 생성물 — packages/contract/src/generate.ts가 만든다.
# Prettier가 다시 포맷하면 `pnpm format` 직후 `pnpm db:check`가 실패한다
# (생성기 출력과 내용이 달라지므로). 형식은 생성기가 책임진다.
db/
docs/ERD.md

# 백엔드는 Prettier 대상이 아니다 (Java/XML은 IDE 포매터가 담당)
apps/api/
gradle/
gradlew
gradlew.bat
