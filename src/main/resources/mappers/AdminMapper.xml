<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.tattoo_project.mapper.AdminMapper">
        <select id="findByEmail" resultType="com.study.tattoo_project.entity.Admin">
            SELECT * FROM admin_tb WHERE email = #{email}
        </select>



</mapper>

<!-- <select id="findAll" resultType="com.study.spingbootservlet.entity.Todo">
        SELECT * FROM todo_tb
    </select>

    <insert id="save" parameterType="com.study.spingbootservlet.entity.Todo">
        INSERT INTO todo_tb (todo_content, todo_complete, importance_flag, create_date)
        VALUES (#{todo_content}, #{todo_complete}, #{importance_flag}, NOW())
    </insert>-->